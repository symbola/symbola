runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 24
    - run: node --version
      shell: bash
    - run: yarn --version
      shell: bash
    - run: yarn install --immutable --immutable-cache
      shell: bash
